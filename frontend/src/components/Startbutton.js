// StartButton.js
import React from 'react';

const Startbutton = ({ onClick }) => {
  return (
    <button onClick={onClick}>
      Start Face Tracking
    </button>
  );
};

export default Startbutton;
