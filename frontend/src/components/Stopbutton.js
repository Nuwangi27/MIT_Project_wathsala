// StopButton.js
import React from 'react';

const Stopbutton = ({ onClick }) => {
  return (
    <button onClick={onClick}>
      Stop Face Tracking
    </button>
  );
};

export default Stopbutton;
